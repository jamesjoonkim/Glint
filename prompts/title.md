Generate a concise thread title (≤60 characters) based on the first capture's content.

Rules:
- Output ONLY the title text. No quotes. No prose. No "Title:" prefix.
- Capture the *what*, not the *type*: "TypeError on user lookup" beats "JavaScript error screenshot".
- Lowercase except for proper nouns and acronyms. Sentence-case is fine if needed.
- 60 chars hard max. Aim for 30-50.
- If the content is genuinely ambiguous, output a short factual descriptor like "screenshot of macOS Settings".
